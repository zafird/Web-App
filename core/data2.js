let htmldata1 = "<ul><li>Artist: Post Malone</li><li>Album: beerbongs & bentleys</li><li>Release Date: 2018.04.27</li></ul>";
let htmldata2 = "<ul><li>Artist: 5 Seconds Of Summer</li><li>Album: Youngblood</li><li>Release Date: 2018.06.15</li></ul>";
let htmldata3 = "<ul><li>Artist: Ariana Grande</li><li>Album: Sweetener</li><li>Release Date: 2018.08.17</li></ul>";
let htmldata4 = "<ul><li>Artist: Gucci Mane X Bruno Mars X Kodak Black</li><li>Album: Wake Up In The Sky</li><li>Release Date: 2018.09.14</li></ul>";
let htmldata5 = "<ul><li>Artist: Ella Mai</li><li>Album: Ella Mai</li><li>Release Date: 2018.10.12</li></ul>";
let htmldata6 = "<ul><li>Artist: Post Malone & Swae Lee</li><li>Album: Sunflower</li><li>Release Date: 2018.10.18</li></ul>";
let htmldata7 = "<ul><li>Artist: Benny Blanco, Halsey & Khalid</li><li>Album: Eastside</li><li>Release Date: 2018.07.13</li></ul>";
let htmldata8 = "<ul><li>Artist: Bad Bunny Featuring Drake</li><li>Album: MIA/li><li>Release Date: 2018.10.12</li></ul>";
let htmldata9 = "<ul><li>Artist: Khalid & Normani</li><li>Album: Love, Simon OST</li><li>Release Date: 2018.04.06</li></ul>";
let htmldata10 = "<ul><li>Artist: Imagine Dragons</li><li>Album: Origins (Deluxe)</li><li>Release Date: 2018.11.09</li></ul>";

let jsondata1 = ["Artist: Ariana Grande", "Album: Thank U, Next", "Release Date: 2018.11.03"]
let jsondata2 = ["Artist: Travis Scot", "Album: ASTROWORLD", "Release Date: 2018.08.03"]
let jsondata3 = ["Artist: Marshmello & Bastille", "Album: Happier", "Release Date: 2018.08.17"]
let jsondata4 = ["Artist: Halsey", "Album: Without Me", "Release Date: 2018.10.04"]
let jsondata5 = ["Artist: Juice WRLD", "Album: F*** You Bella", "Release Date: 2018.05.23"]
let jsondata6 = ["Artist: Panic! At The Disco", "Album: Pray For The Wicked", "Release Date: 2018.06.22"]
let jsondata7 = ["Artist: Sheck Wes", "Album: Mo Bamba", "Release Date: 2018.01.31"]
let jsondata8 = ["Artist: Maroon 5 Featuring Cardi B", "Album: Red Pill Blues (Deluxe)", "Release Date: 2018.06.15"]
let jsondata9 = ["Artist: Lil Baby & Gunna", "Album: Drip Harder", "Release Date: 2018.10.04"]
let jsondata10 = ["Artist: Kodak Black Featuring Travis Scott & Offset", "Album: ZEZE", "Release Date: 2018.10.19"]

module.exports = {
    getHTML: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata1;
    },

    getHTML2: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata2;
    },

    getHTML3: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata3;
    },

    getHTML4: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata5;
    },

    getHTML5: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata5;
    },

    getHTML6: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata6;
    },

    getHTML7: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata7;
    },

    getHTML8: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata8;
    },

    getHTML9: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata9;
    },

    getHTML10: function () {
        console.log("called: getHTML");
        // Note: this could be from a DB, for now it's just hard-coded
        return htmldata10;
    },

    getJSON: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata1;
    },

    getJSON2: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata2;
    },

    getJSON3: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata3;
    },

    getJSON4: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata4;
    },

    getJSON5: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata5;
    },

    getJSON6: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata6;
    },

    getJSON7: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata7;
    },

    getJSON8: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata8;
    },

    getJSON9: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata9;
    },

    getJSON10: function () {
        console.log("called: getJSON");
        // Note: this could be from a DB, for now it's just hard-coded
        return jsondata10;
    }
}

var someOtherFunction = function () {
    console.log("Can't touch this!");
}